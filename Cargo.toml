[package]
name = "ayeos"
version = "0.1.0"
edition = "2021"
description = "ayeOS — ternary matrix inference daemon. CPU: kernel8 (hearth). GPU: MLX-QUANT (brain). Coord: vaked (capability graph)."

[[bin]]
name = "ayeosd"
path = "src/daemon.rs"

[[bin]]
name = "ayeos-matrix"
path = "src/matrix_cli.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
base64 = "0.22"
libc = "0.2"
